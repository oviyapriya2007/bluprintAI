# BlueprintAI — hybrid detection pipeline (backend/) dependencies.
# Python 3.10+

# Stage 1 (ingest.py) reuses document-processor's rasterization -- see
# document-processor/requirements.txt for pdf2image/Pillow/opencv-python.

# Stage 2 (bom_extract.py) -- deterministic BOM table extraction.
pdfplumber>=0.11.0     # vector PDF table extraction (real text)
pytesseract>=0.3.10    # raster OCR fallback -- requires the Tesseract-OCR
                        # binary installed on the system (not pip-installable);
                        # see https://github.com/tesseract-ocr/tesseract
Pillow>=10.0.0

# Stage 3 (callout_detect.py) -- OpenCV balloon/callout candidate detection.
opencv-python>=4.8.0
numpy>=1.24

# Stage 4 (classify.py) -- the only stage that calls Claude.
anthropic>=1.4.0
python-dotenv>=1.0
